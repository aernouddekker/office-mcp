{
  "private": true,
  "workspaces": [
    "packages/imagegen",
    "packages/judge",
    "packages/transcribe"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "clean": "rm -rf packages/*/dist"
  }
}
