{
  "private": true,
  "workspaces": [
    "packages/imagegen",
    "packages/judge",
    "packages/transcribe",
    "packages/print"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "clean": "rm -rf packages/*/dist"
  }
}
